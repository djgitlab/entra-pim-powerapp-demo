# Environment Variables (Recommended)

Use environment variables to avoid hardcoding values.

Examples:

- Feature flags (enable approvals view, show request history)
- Display labels
- Optional configuration to control UX behavior

Document actual variables once finalized.
